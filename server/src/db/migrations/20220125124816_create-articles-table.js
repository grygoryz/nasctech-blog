const { dbTables } = require('../../helpers/constants');

exports.up = function (knex) {
	return knex.schema.createTable(dbTables.articles, function (table) {
		table.specificType('id', 'integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY');
		table.string('heading', 225).defaultTo('Untitled article').notNullable();
		table.string('content', 10000).notNullable();
		table.timestamp('created_at').nullable();
		table.timestamp('updated_at').nullable();
	});
};

exports.down = function (knex) {
	return knex.schema.dropTableIfExists(dbTables.articles);
};
